<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@color/violetbackground"
    android:layout_height="match_parent"
    tools:context=".ShowMyEventsActivity">

    <include
        android:id="@+id/toolbar"
        layout="@layout/toolbar"
        app:layout_constraintBottom_toTopOf="@id/tv_event_fav"
        app:layout_constraintTop_toTopOf="parent"/>

    <TextView
        android:layout_below="@id/toolbar"
        android:visibility="gone"
        android:id="@+id/tv_event_fav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Mes événements favoris"
        android:fontFamily="sans-serif-light"
        android:textSize="25sp"
        android:gravity="center"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:textColor="@color/colorTextTitle"
        />


    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/activity_showmyevents_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/tv_event_fav">

    </androidx.recyclerview.widget.RecyclerView>


</RelativeLayout>